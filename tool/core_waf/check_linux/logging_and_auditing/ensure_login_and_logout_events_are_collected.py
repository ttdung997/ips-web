import os
import re
import sys

# sys.path.insert(0, '/var/log/core_waf/check_security_mysql/')
# import helper

error_list = list()

# def check(username, password):
def check():
    if 1==1:
		error_list.append('[WARNING] ] Linux Secure are not strong enough')
		error_list.insert(0, 2)
        flag = False
        # print(error_list)
    if flag:
        error_list.insert(0, 0)
    return error_list

def fix():
    return

